<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post ? 'Edit' : 'Create' %> Post</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1><%= post ? 'Edit' : 'Create' %> Post</h1>
    <a href="/">Back to Home</a>
  </header>
  <main>
    <form action="<%= post ? '/edit/' + index : '/new' %>" method="POST">
      <label for="title">Title:</label>
      <input type="text" name="title" id="title" value="<%= post ? post.title : '' %>" required>
      
      <label for="content">Content:</label>
      <textarea name="content" id="content" required><%= post ? post.content : '' %></textarea>
      
      <button type="submit"><%= post ? 'Save Changes' : 'Create Post' %></button>
    </form>
  </main>
</body>
</html>
